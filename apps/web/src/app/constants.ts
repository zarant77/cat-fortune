// Economy

export const INITIAL_BALANCE = 1000;
export const DEFAULT_BET = 10;
export const MIN_BET = 1;
export const MAX_BET = 100;

// Animation (shared baseline, can be overridden per client)

export const SPIN_ANIMATION_FRAME_MS = 70;
export const REEL_STOP_DELAY_MS = 140;

// UI limits / helpers

export const MAX_SEED = Number.MAX_SAFE_INTEGER;

export const SPIN_ANIMATION_FRAMES = 12;
